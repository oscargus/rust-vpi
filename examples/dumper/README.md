# dumper example

Example VPI plugin crate that demonstrates dumping simulation information.

## Build

From the workspace root:

```bash
cargo build -p dumper
```

The crate builds as a `cdylib` suitable for loading by a simulator.
