# siminfo example

Example VPI plugin crate that reports simulator information.

## Build

From the workspace root:

```bash
cargo build -p siminfo
```

The crate builds as a `cdylib` suitable for loading by a simulator.
