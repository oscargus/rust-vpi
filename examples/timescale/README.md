# timescale example

Example VPI plugin crate that reads and reports timescale information.

## Build

From the workspace root:

```bash
cargo build -p timescale
```

The crate builds as a `cdylib` suitable for loading by a simulator.
